# secrets — never commit
.env
.env.*

# local, machine-specific Claude Code settings
.claude/settings.local.json

# raw analytical source (642MB — exceeds GitHub limits; build-side only, see docs/data-contract.md)
raw_data/*.parquet

# extracted skill bundle + local pmtiles copy (large, re-downloadable)
_extracted/
claude-dataviz-marketplace.zip
data-prep/Health_Zip_converted.pmtiles
data-prep/tile_health.parquet
data-prep/.venv/
__pycache__/
*.pyc

# next / node
web/node_modules/
web/.next/
web/out/
web/test-results/
web/playwright-report/
web/verify-shots/
web/tsconfig.tsbuildinfo
